{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "functions": {
    "api/chat.js": {
      "regions": ["fra1"],
      "maxDuration": 60
    }
  }
}
